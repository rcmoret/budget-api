---
category: Actions
---

# EditButton

Small circular pencil (✎) toggle that reveals a card's inline edit form. Mirrored horizontally (`rotate-y-180`) so the pencil points the expected way.

```jsx
<CardRow><EditButton showForm={() => setEditing(true)} /></CardRow>
```
