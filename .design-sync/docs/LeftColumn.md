---
category: Navigation
---

# LeftColumn

The application's primary navigation column — account menu, budget links and the app-config items. Reads the navigation and app-config stores; composed automatically by `PageComponent`.
