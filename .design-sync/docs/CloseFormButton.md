---
category: Actions
---

# CloseFormButton

Small circular ✕ toggle that dismisses a card's inline edit form — the counterpart to `EditButton`. Both share the same `FormToggleButton` base styling.

```jsx
<CardRow><CloseFormButton onDismiss={() => setEditing(false)} /></CardRow>
```
