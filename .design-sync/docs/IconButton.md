---
category: Actions
---

# IconButton

Circular 8×8 translucent-white button that wraps arbitrary icon children. Used for inline row actions where a full button would be too heavy.

```jsx
<IconButton title="Edit item" onClick={showForm}><Pencil /></IconButton>
```
