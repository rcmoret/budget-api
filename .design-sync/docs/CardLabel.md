---
category: Cards
---

# CardLabel

A card's title row — large label on the left, caller's children (usually actions) on the right, underlined with a primary hairline. Pass `dragAttributes`/`dragListeners` from dnd-kit to reveal the ⠿ drag handle.

```jsx
<CardLabel label="Groceries"><EditButton showForm={showForm} /></CardLabel>
```
