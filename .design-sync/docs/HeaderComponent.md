---
category: Layout
---

# HeaderComponent

Page title plus an optional right-hand slot, intended for `PageComponent`'s `header`. Falls back to "Budget Application" when given neither children nor `title`.

```jsx
<HeaderComponent title="Accounts" rightColumnComponent={<NeighborLinks … />} />
```
