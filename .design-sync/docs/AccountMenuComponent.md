---
category: Navigation
---

# AccountMenuComponent

Expandable account switcher in the left column — a primary account link plus a collapsible list of the remaining accounts. Reads the account-navigation store and uses Inertia `Link`.
