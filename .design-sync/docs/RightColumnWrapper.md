---
category: Layout
---

# RightColumnWrapper

Bordered, rounded, padded container for right-column content. Returns `null` when it has no children, so an empty aside collapses instead of leaving an empty box.

```jsx
<RightColumnWrapper>{detailPanel}</RightColumnWrapper>
```
