---
category: Cards
---

# UnarchiveIcon

Ghost button with an ⚒ glyph — the inverse of `ArchiveIcon`. Already composed into `ArchivedItemCard`, so you rarely place it yourself.

```jsx
<UnarchiveIcon title="Restore account" onClick={unarchive} />
```
