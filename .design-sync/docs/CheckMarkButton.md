---
category: Actions
---

# CheckMarkButton

Round confirm button showing a ✓ glyph. With `type="submit"` the shared `.round-cta` rule paints it with the success token, so submit buttons read green automatically.

```jsx
<CheckMarkButton type="submit" title="Save" ariaLabel="Save changes" />
```
