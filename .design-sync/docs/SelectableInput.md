---
category: Forms
---

# SelectableInput

Controlled checkbox row. Same row treatment as `RadioInput` — full-width clickable label, outline when checked — but allows multiple selections.

```jsx
<SelectableInput value="clear" checked={clear} onChange={() => setClear(!clear)}>
  Clear on rollover
</SelectableInput>
```
