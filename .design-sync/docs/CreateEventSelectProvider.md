---
category: Forms
---

# CreateEventSelectProvider

Context provider for `CreateEventSelectComponent` — owns the fetched event list, the amount field, validation errors and the submit handler. Requires Inertia and the app-config route store.
